<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniVoice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.UniVoice" parent="Base.Theme.UniVoice" />

    <!--Splash-->
    <style name="Theme.UniVoice.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/font_W01</item> <!--배경 색상-->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_background</item> <!--아이콘-->
        <item name="windowSplashScreenAnimationDuration">3000</item> <!--지속 시간-->
        <item name="postSplashScreenTheme">@style/Base.Theme.UniVoice</item> <!--스플래시 화면 이후 적용할 테마 지정-->
    </style>
</resources>